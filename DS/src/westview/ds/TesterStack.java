package westview.ds;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;

class TesterStack {

	@Test
	void test() {
//		Stack<Integer> myStack = new Stack<Integer>();
//
//		// an empty stack should return a size of 0
//		assert (myStack.size() == 0); // nothing added
//
//		// can we try to pop?
//		assert (myStack.pop() == null);
//
//		// write an assertion about the peek method
//		// that should be true when the stack is empty
//		assert (myStack.peek() == null); // empty
//
//		// push to the stack twice
//		// make an assertion about what should be true
//		myStack.push(7);
//		myStack.push(11);
//		assert (myStack.size() == 2);
//		assert (myStack.peek() == 11);// 11 is at the top
//
//		// destructive testing
//		assert (myStack.pop() == 11);
//		
//		//check that the size changed and that the new top is what is expected
//		assert(myStack.peek()==7);
//		
//		Queue<Integer> queue = new Queue<Integer>();
//		assert (queue.removeQ() == null);
//		queue.addQ(10);
//		queue.addQ(16);
//		assert (queue.size() == 2);
//		assert (queue.peek() == 10);
		
//		LinkedList a = new LinkedList();
//		Node n = new Node(data);
//		a.add()
		
	}

}
